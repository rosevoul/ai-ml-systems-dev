# faye-recsys-models — Python dependencies
# Install: pip install -r requirements.txt

# Core data
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
tqdm>=4.65.0

# Deep learning (Two-Tower, MBAR, Rank Transformer)
torch>=2.0.0

# Ranking
xgboost>=1.7.0         # optional: falls back to sklearn if missing

# Approximate nearest-neighbour retrieval
faiss-cpu>=1.7.4       # optional: falls back to numpy cosine if missing

# Dimensionality reduction for 2D embedding scatter
umap-learn>=0.5.3      # optional: falls back to sklearn PCA if missing

# RAG (optional upgrade from TF-IDF to dense retrieval)
# sentence-transformers>=2.2.0
